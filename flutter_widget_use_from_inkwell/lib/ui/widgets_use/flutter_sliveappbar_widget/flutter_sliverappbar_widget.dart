import 'package:flutter/material.dart';

class FlutterSliverappbarWidget extends StatelessWidget {
  const FlutterSliverappbarWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: CustomScrollView(

      )
    );
  }
}
