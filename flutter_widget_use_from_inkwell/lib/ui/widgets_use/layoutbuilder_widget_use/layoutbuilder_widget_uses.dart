import 'package:flutter/material.dart';

class LayoutbuilderWidgetUses extends StatelessWidget {
  const LayoutbuilderWidgetUses({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('LayoutBuilder use')
      ),
      body: SingleChildScrollView(
        child:Column(
          children: [

          ],
        )
      )
    );
  }
}
