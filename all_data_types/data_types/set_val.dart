void main() {
  ///Set is am unordered list
  /// we can’t store duplicate data.
  ///memory taking depends on set items type
  Set<int> numbers = {1, 2, 3, 4, 5, 6};
  print(numbers);
  var newList = numbers.toList();
  print(newList[3]);
}
