void main() {
  //List is a collection of Ordered object
  //we can add duplicate data in it
  //it takes memories as it need.
  //it depends on the elements of it.
  List<int> numbers = [1, 2, 3, 3, 3, 4, 4, 5, 6, 7, 8];
  print(numbers);
  print(numbers[4]);
}
